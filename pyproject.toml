[tool.poetry]
name = "kicad-draw"
version = "0.2.0"
description = ""
authors = ["t-sasatani <sasatani.dev@gmail.com>"]
readme = "README.md"
packages = [{include = "kicad_draw"}]

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^1.25.2"

[tool.poetry.group.dev.dependencies]
ipykernel = "^6.25.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
